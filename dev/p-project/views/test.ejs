<html>
    <head>
        <meta charset="UTF-8"> 
    </head>
    <body>
        <h1>Hello EJS World </h1>
        <ul>
            <% goodslist.forEach(function (good, index) { %>
                <div>
                    <div><%= good.goo_name %></div>
                    <div><%= good.goo_type %></div>
                    <div><%= good.goal_price %></div>
                </div>
            <% }); %>
        </ul> 
    </body>
</html>